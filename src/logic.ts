import type{Rating,Review,State,Word}from'./types';const DAY=864e5;
export const initial=(wordId:string,now=new Date()):Review=>({wordId,due:now.toISOString(),interval:0,ease:2.5,repetitions:0,lapses:0});
export function schedule(p:Review,r:Rating,now=new Date()){let{interval,ease,repetitions,lapses}=p;if(r===3)return{...p,mastered:true,repetitions:repetitions+1,due:'9999-12-31T23:59:59.999Z'};if(r===0){interval=.02;repetitions=0;lapses++;ease=Math.max(1.3,ease-.2)}else if(r===1){interval=Math.max(1,interval*1.2||1);ease=Math.max(1.3,ease-.15);repetitions++}else{interval=repetitions===0?1:repetitions===1?3:Math.max(1,interval*ease);repetitions++}return{...p,mastered:false,interval:+interval.toFixed(2),ease:+ease.toFixed(2),repetitions,lapses,due:new Date(now.getTime()+interval*DAY).toISOString()}}
export function distance(a:string,b:string){const d=Array.from({length:a.length+1},()=>Array(b.length+1).fill(0));for(let i=0;i<=a.length;i++)d[i][0]=i;for(let j=0;j<=b.length;j++)d[0][j]=j;for(let i=1;i<=a.length;i++)for(let j=1;j<=b.length;j++)d[i][j]=Math.min(d[i-1][j]+1,d[i][j-1]+1,d[i-1][j-1]+ +(a[i-1]!==b[j-1]));return d[a.length][b.length]}
export function graph(ws:Word[],s:State){return ws.flatMap((a,i)=>ws.slice(i+1).map(b=>{const d=distance(a.word,b.word),m=s.mistakes.filter(x=>x.from===a.id&&x.to===b.id||x.from===b.id&&x.to===a.id).reduce((n,x)=>n+x.count,0),score=(d<=Math.max(1,Math.floor(Math.max(a.word.length,b.word.length)*.25))?1-d/Math.max(a.word.length,b.word.length):0)+Math.min(1,m*.25);return{a,b,d,m,score}})).filter(x=>x.score>0).sort((a,b)=>b.score-a.score)}
